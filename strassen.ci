mainmodule strassen {
message ValueMsg;
message AddSubMsgArg;
message StrassenSubMsgArg;
message StrassenMsgArg;

	mainchare Main {  
	entry Main(CkArgMsg *m);
	entry [threaded] void run(int size_);
	};

	chare strassen {  
	entry strassen(vector<vector<int>> A, vector<vector<int>> B,int size,CkFuture f);
	entry [threaded] void run(vector<vector<int>> A, vector<vector<int>> B,int size,CkFuture f);
	entry strassen(StrassenMsgArg*);
	entry [threaded] void run(StrassenMsgArg*);
	};  

	chare strassenSub {  
	entry strassenSub(StrassenSubMsgArg *);
	entry [threaded] void run(StrassenSubMsgArg *);
	};  

	chare addition {  
	entry addition(AddSubMsgArg* addSubMsgArg);
	entry [threaded] void run(AddSubMsgArg* addSubMsgArg);
	};

	chare substraction {  
	entry substraction(AddSubMsgArg* addSubMsgArg);
	entry [threaded] void run(AddSubMsgArg* addSubMsgArg);
	};

 }